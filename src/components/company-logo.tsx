import React from 'react';

export const CompanyLogo = ({ className }: { className?: string }) => (
  <div className={`flex items-center justify-center ${className}`}>
    <div className="text-2xl font-bold text-blue-600">Mindspire</div>
  </div>
);
